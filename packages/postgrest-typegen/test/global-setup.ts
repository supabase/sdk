/**
 * Global test setup. Integration/parity tests (added in later phases) use
 * testcontainers to spin up PostgreSQL and will prewarm the image here.
 * Currently a no-op while only pure unit tests exist.
 */
export {};
