name: dart_symbol_extractor
description: >-
  Extracts the public API surface of Dart and Flutter packages into the
  ParseResult shape consumed by the SDK capability matrix check.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  analyzer: ^14.3.0
  path: ^1.9.0

dev_dependencies:
  supabase_lints: ^0.1.2
  test: ^1.25.0
