# yaml-language-server: $schema=../schema/capability-matrix.schema.json
area: functions
title: Edge Functions
description: Invoke Supabase Edge Functions from the client.
groups:
  - id: invocation
    title: Invocation
features:
  - id: functions.invocation.invoke
    name: Invoke
    description: Invokes a function
    group: invocation
  - id: functions.invocation.set_auth
    name: Set Auth
    description: Updates the authorization header
    group: invocation
